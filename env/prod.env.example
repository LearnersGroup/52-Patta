# Production Environment
# Copy to prod.env and fill in real values
# NEVER commit prod.env to git
NODE_ENV=production
PORT=4000

# MongoDB - separate production database
MONGO_DB_NAME=patta_prod
MONGO_USERNAME=your_prod_username
MONGO_PASSWORD=your_prod_password
MONGO_HOST=mongodb%2Bsrv://user:pass@your-cluster.mongodb.net/patta_prod

# Auth - generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_prod_jwt_secret

# CORS - production frontend URL
CORS_ORIGINS=https://yourdomain.com

# Sentry
# SENTRY_DSN=https://your-sentry-dsn
