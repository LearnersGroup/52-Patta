{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(cat | jq -r '.tool_input.file_path // empty') && case \"$FILE_PATH\" in *.env|*.env.*) echo 'BLOCKED: Do not edit .env files — they contain secrets. Use env/*.example templates instead.' >&2; exit 2;; esac"
          }
        ]
      }
    ]
  }
}
