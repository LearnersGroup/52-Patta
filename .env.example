# ===========================================
# 52-Patta Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# cp .env.example .env

# --- MongoDB ---
MONGO_DB_NAME=sequence_db
MONGO_USERNAME=your_mongo_username
MONGO_PASSWORD=your_mongo_password
MONGO_HOST=mongodb%2Bsrv://username:password@your-cluster.mongodb.net/your_db_name

# --- Authentication ---
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here

# --- OAuth (Google) ---
# Get these from https://console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:4000/api/oauth/google/callback

# --- OAuth (Facebook) ---
# Get these from https://developers.facebook.com → My Apps → Settings → Basic
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret
FACEBOOK_CALLBACK_URL=http://localhost:4000/api/oauth/facebook/callback

# --- Client URL (where to redirect after OAuth) ---
CLIENT_URL=http://localhost:3000

# --- Server ---
PORT=4000

# --- CORS (optional, comma-separated origins) ---
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# --- Error Tracking (optional) ---
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# --- Environment ---
# NODE_ENV=development

# --- Client (set in client/.env) ---
# REACT_APP_WS_URL=http://localhost:4000
# REACT_APP_BASE_URL=http://localhost:4000/api
